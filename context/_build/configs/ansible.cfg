# Used during EE image build (galaxy stage). token_path is inside the build container
# after COPY in prepend_galaxy; place your offline token in galaxy-rh-token (see execution-environment.yml).
[defaults]

[galaxy]
server_list = rh_certified, rh_validated, community

[galaxy_server.rh_certified]
url = https://console.redhat.com/api/automation-hub/content/published/
auth_url = https://sso.redhat.com/auth/realms/redhat-external/protocol/openid-connect/token
token_path = /etc/ansible/rh_token

[galaxy_server.rh_validated]
url = https://console.redhat.com/api/automation-hub/content/validated/
auth_url = https://sso.redhat.com/auth/realms/redhat-external/protocol/openid-connect/token
token_path = /etc/ansible/rh_token

[galaxy_server.community]
url = https://galaxy.ansible.com/
