# Optional template: merge into ansible/inventory/hosts.ini or use as a second inventory file.
# Set ansible_host from Terraform:
#   cd terraform && terraform output -json public_ips

[web]
myhost ansible_host=203.0.113.10 ansible_user=ec2-user ansible_ssh_private_key_file=~/.ssh/id_rsa
