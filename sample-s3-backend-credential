# Optional: only if you configure Terraform S3 backend via a partial config file instead of Ansible.
# Default flow uses deploy_vars.yml + local state, then migrate-state-to-s3.sh.
bucket = "REPLACE_WITH_YOUR_TERRAFORM_STATE_BUCKET"
key = "path/to/terraform-state-file"
region = "us-east-1"
access_key = "REPLACE_WITH_AWS_ACCESS_KEY_ID"
secret_key = "REPLACE_WITH_AWS_SECRET_ACCESS_KEY"
