# Template only — copy to a gitignored file (see s3-backend-credentials) and set real values.
# Do not commit access keys. AWS keys and bucket settings live in .env / s3-backend-credentials.
bucket = "REPLACE_WITH_YOUR_TERRAFORM_STATE_BUCKET"
key = "path/to/terraform-state-file"
region = "us-east-1"
access_key = "REPLACE_WITH_AWS_ACCESS_KEY_ID"
secret_key = "REPLACE_WITH_AWS_SECRET_ACCESS_KEY"
